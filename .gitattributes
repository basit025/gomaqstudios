# Normalize line endings: LF in the repo, native in the working copy.
# Keeps diffs clean between Windows checkouts and Linux CI / Vercel builds.
* text=auto eol=lf

# Binary assets — never touch these.
*.jpeg binary
*.jpg  binary
*.png  binary
*.svg  text eol=lf
*.woff binary
*.woff2 binary
